import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capacitor-share-plugins-bugs',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
